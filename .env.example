# Captcha Solver 服务端配置
HOST=0.0.0.0
PORT=15666
OCR_RETRY=3
ADMIN_KEY=changeme

# 日志级别: debug / info / warning / error
LOG_LEVEL=info

# 缓存大小上限
CACHE_LIMIT=1000

# 推理超时（毫秒）
INFER_TIMEOUT=30000

# 限流：每分钟最大请求数，0 表示不限流
RATE_LIMIT_PER_MINUTE=120
